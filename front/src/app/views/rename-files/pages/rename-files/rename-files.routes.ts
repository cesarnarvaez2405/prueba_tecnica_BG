import { Route } from '@angular/router';
import { RenameFilesComponent } from './rename-files.component';

export const RENAME_FILES_PAGE_ROUTES: Route[] = [
  {
    path: '',
    component: RenameFilesComponent,
  },
];
